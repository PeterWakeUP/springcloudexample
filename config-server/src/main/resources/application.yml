server:
  port: 3100

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:3000/eureka/

spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://git.lug.ustc.edu.cn/config/client-my-config.git
          #search-paths: respo1,respo2
          #username: *****@****.com    #如果是private的仓库，则需要用户名和密码
          #password: ****
      #label: master



